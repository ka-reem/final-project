services:
  - type: web
    name: your-game-name
    env: static
    buildCommand: |
      apt-get update && apt-get install -y ncurses-bin
      npm install
      npm run build
    staticPublishPath: ./dist
