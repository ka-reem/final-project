services:
  - type: web
    name: your-game-name
    env: static
    buildCommand: |
      apt-get update && apt-get install -y ncurses-bin
      chmod +x gradlew
      ./gradlew build
    staticPublishPath: ./build/libs
    envVars:
      - key: JAVA_VERSION
        value: 17
